version: "3.8"

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb

    volumes:
      - data:/data/db

  auth_app:
    build: .
    image: auth_image:latest
    container_name: auth_app

    ports:
      - 8080:8000

    depends_on:
      - mongodb

    volumes:
      - static:/app/static

volumes:
  data:
  static:
